// Write a function calculateArea(length, width) that returns the area of a rectangle.

function calculateArea(len, wid){
    const result = 2 * (len + wid)
    console.log(result)
}

calculateArea(2, 3)